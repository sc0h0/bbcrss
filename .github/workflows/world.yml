name: Scrape latest world RSS

on:
  push:
  workflow_dispatch:
  schedule:
    - cron:  '* * * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Install xsltproc
      run: sudo apt-get install xsltproc
    - name: Check out this repo
      uses: actions/checkout@v2
    - name: Fetch latest world RSS
      run: |-
        curl http://feeds.bbci.co.uk/news/world/rss.xml > world.xml
    - name: Run XSLT
      run: |-
        xsltproc rss.xslt world.xml > world.md
    - name: Run XSLT (no date)
      run: |-
        xsltproc rss_nodate.xslt world.xml > world_nodate.md
    - name: Commit and push if it changed
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest world RSS: ${timestamp}" || exit 0
        git push
